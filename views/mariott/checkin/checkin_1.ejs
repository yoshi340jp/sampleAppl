<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Check-in</title>

    <!-- Bootstrap core CSS -->
    <link href="<%= static_path + "/bootstrap/css/theme/" + theme + "/bootstrap.css" %>" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="<%= static_path + "/bootstrap/css/jumbotron-narrow.css" %>" rel="stylesheet">
  </head>

  <body>
	<div>
		<lable><%= error_msg %></label>
	</div>

    <form action="<%= '/' + hotel_id + '/checkin/checkin2' %>" method="post">
      <div>
          <label>Gender</label>
          <input type="radio" id="gender" name="gender" value="male"/>男
          <input type="radio" id="gender" name="gender" value="femail"/>女          
      </div>
      <div>
          <label>Room Number</label>
          <input type="text" id="roomNo" name="roomNo"/>
      </div>
      <div>
          <input type="submit" id="next" value="Proceed"/>
      </div>
  </form>    
  </body>
</html>

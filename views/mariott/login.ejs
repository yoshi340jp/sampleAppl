<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Login Screen</title>

    <!-- Bootstrap core CSS -->
    <link href="<%= static_path + "/bootstrap/css/theme/" + theme + "/bootstrap.css" %>" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="<%= static_path + "/bootstrap/css/jumbotron-narrow.css" %>" rel="stylesheet">
  </head>

  <body>
	<div>
		<lable><%= error_msg %></label>
	</div>
  	<div>
  		<%- __('login.header') %>
  	</div>
    <form action="<%= '/' + hotel_id + '/loginauth'%>" method="post">
      <div>
          <label><%= __('login.userName') %></label>
          <input type="text" id="username" name="username"/>
      </div>
      <div>
          <label><%= __('login.password') %></label>
          <input type="password" id="password" name="password"/>
      </div>
      <div>
          <input type="submit" id="login" value="<%= __('login.proceed') %>"/>
      </div>
  </form>
  </body>
</html>
